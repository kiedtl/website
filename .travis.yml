language: bash

os:
    - linux

before_script:
    - sudo npm install --global surge
    - sudo apt-get update
    - sudo apt-get install imagemagick pngquant jpegoptim webp

script:
    - chmod +x src/mksite
    - chmod +x src/deploy
    - src/mksite
    - src/deploy
